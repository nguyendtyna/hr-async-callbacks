const $ = require('jquery');

module.exports.getWord = (callback) => {
  $.ajax({
    
  });
};

module.exports.sendGuess = (letter, callback) => {
  $.ajax({
    
  });
};

module.exports.retrieveScore = (callback) => {
  $.ajax({
    
  });
};
